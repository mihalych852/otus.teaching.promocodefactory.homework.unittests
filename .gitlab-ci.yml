image: mcr.microsoft.com/dotnet/core/sdk:7.0

stages:
    - build
    - test

before_script:
    - cd src
    - dotnet restore

build:
    stage: build
    script:
        - dotnet build

test:
    stage: test
    script:
        - dotnet test
